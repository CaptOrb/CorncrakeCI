# Schemas

## Gitea API

Downloaded from <https://docs.gitea.com/api/1.24/> (Download button on page).

Currently OpenAPI 2.0, so `swagger2openapi` is used to convert it to
OpenAPI 3.0 before generating code.
